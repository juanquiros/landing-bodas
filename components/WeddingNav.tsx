"use client";
import { useEffect, useState } from "react";
const items=[['inicio','Inicio'],['nosotros','Nosotros'],['evento','El gran día'],['cronograma','Fiesta'],['dress-code','Dress Code'],['musica','Tu tema'],['regalo','Regalo'],['galeria','Momentos'],['rsvp','Confirmar asistencia']] as const;
export function WeddingNav(){const [open,setOpen]=useState(false);const [active,setActive]=useState('inicio');useEffect(()=>{const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)setActive(e.target.id)}),{rootMargin:'-40% 0px -48% 0px'});items.forEach(([id])=>{const el=document.getElementById(id);if(el)observer.observe(el)});return()=>observer.disconnect()},[]);useEffect(()=>{const esc=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',esc);return()=>window.removeEventListener('keydown',esc)},[]);const go=(id:string)=>{setOpen(false);document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',`#${id}`)};return <nav className="wedding-nav" aria-label="Navegación de la invitación"><button className="nav-trigger" aria-label="Abrir menú" aria-expanded={open} aria-controls="wedding-menu" onClick={()=>setOpen(true)}><b>K & M</b><span>☰</span></button><div className="nav-dots">{items.slice(0,7).map(([id,label])=><button key={id} className={active===id?'active':''} onClick={()=>go(id)} aria-label={label}><i/><span>{label}</span></button>)}</div>{open&&<div id="wedding-menu" className="nav-overlay" role="dialog" aria-modal="true" aria-label="Menú de navegación"><button className="nav-close" onClick={()=>setOpen(false)} aria-label="Cerrar menú">×</button><p>K & M</p><small>21 · 11 · 2026</small><div>{items.map(([id,label],i)=><button key={id} onClick={()=>go(id)}><small>{String(i+1).padStart(2,'0')}</small>{label}</button>)}</div></div>}</nav>}
